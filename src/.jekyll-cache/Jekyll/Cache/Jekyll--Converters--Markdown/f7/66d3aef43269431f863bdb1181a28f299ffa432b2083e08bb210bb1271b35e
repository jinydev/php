I"�<h1 id="자동-전역변수">자동 전역변수</h1>
<hr />
<p>자동 전역변수는 phpinfo() 함수를 통해 확인할 수 있습니다.</p>

<p>예제 파일 info-02.php</p>
<div class="language-php highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="cp">&lt;?php</span> 
 	<span class="k">echo</span> <span class="s2">"casting &lt;br&gt;"</span><span class="p">;</span>

 	<span class="nv">$a</span> <span class="o">=</span> <span class="s2">"변수를 삭제합니다."</span><span class="p">;</span> 
 	<span class="k">echo</span> <span class="nv">$a</span><span class="mf">.</span><span class="s2">"&lt;br&gt;"</span><span class="p">;</span>
 	<span class="k">echo</span> <span class="s2">"변수 삭제 = "</span><span class="mf">.</span><span class="p">(</span><span class="k">unset</span><span class="p">)</span><span class="nv">$a</span><span class="p">;</span> <span class="c1">// 결과:  </span>
 <span class="cp">?&gt;</span>
</code></pre></div></div>

<p>화면 출력</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code></code></pre></div></div>

<p>자동 전역변수는 PHP에서 미리 설정되어 있는 변수입니다. 하지만 전역변수들은 서버의 정보를 포함한 보완적인 민감한 내용도 같이 출력됩니다.</p>

<p>PHP 4.2.0 이후부터는 환경 설정 파일 php.ini에서 register_global 값을 기본 off로 설정합니다.</p>

<p><br /><br /></p>
:ET